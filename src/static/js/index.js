var container = document.querySelector('#js-message');

setTimeout(function(){
 container.innerText = 
    'This document defines a method, INFO, for the Session Initiation Protocol (SIP)';
}, 2000);

